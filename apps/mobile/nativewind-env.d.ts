/// <reference types="nativewind/types" />
// expo-env.d.ts は .gitignore 対象（expo start が生成する）ため、
// クリーンチェックアウトでも *.css の side-effect import や react-native-web の型が解決するようここで参照する
/// <reference types="expo/types" />
